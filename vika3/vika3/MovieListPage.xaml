<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="vika3.MovieListPage" Title="Name list">
	<ListView x:Name="listview" ItemsSource="{Binding AllMovies}" HasUnevenRows="True" ItemSelected="Listview_OnItemSelected">
		 <ListView.ItemTemplate >
		      <DataTemplate>
		        <ViewCell>
		          <Grid Padding="10,5">
		            <Grid.RowDefinitions>
		              <RowDefinition Height="Auto"></RowDefinition>
		              <RowDefinition Height="*"></RowDefinition>
		            </Grid.RowDefinitions>
		            <Grid.ColumnDefinitions>
		              <ColumnDefinition Width="*"></ColumnDefinition>
		              <ColumnDefinition Width="40"></ColumnDefinition>
		            </Grid.ColumnDefinitions>
		            <Label Grid.Row="0" Grid.Column="0"
		                   Text="{Binding TitleYear}"
		                   FontAttributes="Bold"
		                   FontSize="Medium"
		                   TextColor="#1f03e1"/>
		            <Label Grid.Row="1" Grid.Column="0"
		                   Text="{Binding Actors}"
		                   FontSize="Small"/>
		          </Grid>
		        </ViewCell>
		        
		      </DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentPage>
